{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node --env-file=.env --watch server.js",
    "db:generate" : "drizzle-kit generate",
    "db:migrate" : "drizzle-kit migrate",
    "db:studio" : "drizzle-kit studio",
    "seed:categories" : "node --env-file=.env src/seed/categories.seed.js",
    "seed:products": "node --env-file=.env src/seed/products.seed.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "drizzle-orm": "^0.45.1",
    "express": "^5.2.1",
    "mysql2": "^3.16.3"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.8"
  }
}
